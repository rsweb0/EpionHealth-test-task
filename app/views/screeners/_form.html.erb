<p>
  Over the past 2 weeks, how often have you been bothered by any of the following problems?
</p>
<p>
  <%= form_for @screener, url: check_in_screeners_path(@check_in), remote: true do |f| %>
    <% Screener::QUESTIONS.each_with_index do |question, index| %>
      <%= f.fields_for :responses do |response_fields| %>
        <div>
          <p><%= index + 1 %>. <%= question %></p>
          <%= response_fields.hidden_field :question, value: question %>
          <%= response_fields.collection_radio_buttons(:answer, Screener::ANSWER_OPTIONS, :first, :last) %>
        </div>
      <% end %>
    <% end %>
    <br>
    <%= f.submit "Submit" %>
  <% end %>
</p>
